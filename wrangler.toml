# Cloudflare Workers static assets
# https://developers.cloudflare.com/workers/static-assets/
#
# Account ID comes from the CLOUDFLARE_ACCOUNT_ID environment variable
# (GitHub Actions secret or local shell). Do not commit account IDs here.

name = "allforummah-website"
main = "worker.js"
compatibility_date = "2026-07-11"

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
run_worker_first = ["/api/cms/*"]
